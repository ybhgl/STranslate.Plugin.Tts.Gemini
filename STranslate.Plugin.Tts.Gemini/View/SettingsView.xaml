<UserControl
    x:Class="STranslate.Plugin.Tts.Gemini.View.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:plugin="clr-namespace:STranslate.Plugin;assembly=STranslate.Plugin"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vm="clr-namespace:STranslate.Plugin.Tts.Gemini.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <!-- 设置页面主容器 -->
    <ikw:SimpleStackPanel Spacing="12">

        <!-- API URL 配置 -->
        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Tts_Gemini_Url}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Globe_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <TextBox
                MinWidth="300"
                ui:ControlHelper.PlaceholderText="{DynamicResource STranslate_Plugin_Tts_Gemini_Url_Placeholder}"
                Text="{Binding Url}" />
        </ui:SettingsCard>

        <!-- API Key 配置 -->
        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Tts_Gemini_ApiKey}">
            <ui:SettingsCard.Description>
                <ui:HyperlinkButton Content="{DynamicResource STranslate_Plugin_Tts_Gemini_ApiKey_Description}" NavigateUri="https://aistudio.google.com/app/apikey" />
            </ui:SettingsCard.Description>
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Key_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <PasswordBox
                MinWidth="300"
                plugin:PasswordBoxAssistant.Attach="True"
                plugin:PasswordBoxAssistant.Password="{Binding ApiKey}"
                ui:ControlHelper.PlaceholderText="{DynamicResource STranslate_Plugin_Tts_Gemini_ApiKey_Placeholder}" />
        </ui:SettingsCard>

        <!-- 模型配置 -->
        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Tts_Gemini_Model}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Bot_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <TextBox
                MinWidth="200"
                ui:ControlHelper.PlaceholderText="gemini-2.5-flash-preview-tts"
                Text="{Binding Model}" />
        </ui:SettingsCard>

        <!-- 音色选择配置 -->
        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Tts_Gemini_Voice}">
            <ui:SettingsCard.Description>
                <ui:HyperlinkButton Content="{DynamicResource STranslate_Plugin_Tts_Gemini_Voice_Description}" NavigateUri="https://ai.google.dev/gemini-api/docs/generate-content/speech-generation#voices" />
            </ui:SettingsCard.Description>
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.PersonVoice_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <ComboBox
                MinWidth="200"
                DisplayMemberPath="Name"
                ItemsSource="{Binding Voices}"
                SelectedItem="{Binding SelectedVoice}" />
        </ui:SettingsCard>

        <!-- 支持信息 -->
        <ui:SettingsCard Description="{DynamicResource STranslate_Plugin_Tts_Gemini_Support_Description}" Header="{DynamicResource STranslate_Plugin_Tts_Gemini_Support}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.PersonSupport_20_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <ui:HyperlinkButton Content="Google Gemini" NavigateUri="https://ai.google.dev/" />
        </ui:SettingsCard>

    </ikw:SimpleStackPanel>
</UserControl>